[pytest]
# Only collect the plugin's own tests; the vendored libraries ship their own
# test suites (win32ctypes, etc.) that fail to import outside their packages.
testpaths = tests
norecursedirs = vendor
